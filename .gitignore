node_modules
out
dist
.DS_Store
*.local
release/
out/
artefacts/
# Compiled native helpers — derived from their .swift sources by `npm run build:native` (and dist:mac).
native/ocr
native/media

# Agent scratch: SDD briefs, reports and local parity baselines
.superpowers/

# agent scratch probes (never commit)
.tw-*
docs/layout-doctor/
