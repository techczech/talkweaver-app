# Clean imported slides from {{TALK_TITLE}}

Pack: `{{PACK_PATH}}`

Selected slides: {{SLIDES}}

Cleanup passes: {{PASSES}}

Read `AGENTS.md` and the files under `instructions/` before working. Inspect the original render, extracted source, deterministic decision and current Markdown for every selected slide. Write suggestions only in `suggestions/` using the documented JSON format.
